
export const AddPatientNotes = () => {
    // const { subjectOptionsData, fetchData } = SubjectTypeOptionsHook();

    // useEffect(() => {
    //     // This effect will triggered to fetch notes subject options
    //     props.isModalState && fetchData();
    // }, [props.isModalState]);

    return (
        <>
           Hi form add pateint ntoes
        </>
    )
}